# 포켓몬 도감 퀴즈 게임 — 계획서 (plan.md)

## 1. 목표

책 **《포켓몬스터 썬&문 포켓몬 전국대도감》**(1~7세대, 802마리)을 읽은 7살 아이가
**책 내용을 떠올리며 퀴즈를 풀고, 맞출 때마다 나만의 도감이 채워지는** 웹 게임.

- 퀴즈 단서(도감 설명·타입·별명·크기·고향·특성·진화)를 **소리내어 읽는 것 자체가 읽기 연습**이 되도록 구성
- 도감을 한 마리씩 모으는 **수집의 재미**가 핵심 동기
- 책과 연동: 포켓몬마다 "책 ○○쪽에서 찾아봐!" 표시 (인덱스 사진 3장에서 추출)

## 2. 인터뷰로 확정된 사항

| 항목 | 결정 |
|---|---|
| 플랫폼 | **웹앱** — HTML/JS, 패드 브라우저에서 실행. 설치 불필요 |
| 정답 입력 | **글자 타일 조합** — 정답 글자 + 함정 글자를 큰 버튼으로 섞어 제시, 순서대로 터치해 이름 완성 |
| 도감 범위 | **전체 802마리**, 지역(세대)별 챕터로 순차 오픈 |
| 데이터 | **PokéAPI에서 미리 다운로드해 내장** — 플레이 시 인터넷 불필요, 로딩 빠름 |
| 문제 화면 | **실루엣 → 정답 시 컬러 공개** ("이 포켓몬은 누구지?" 스타일) |
| 오답 처리 | **3번 틀리면 "도망갔다!"** 처리 후 다음 문제로. 도망간 포켓몬은 나중에 다시 등장 |

## 3. 게임 구성

### 3.1 화면 흐름

```
[홈] ─── 지역(챕터) 선택 ───→ [퀴즈 화면] ──정답──→ [포획 연출 + 도감 등록]
  │                              │ 3회 오답              │
  └──→ [내 도감]                 └─→ "도망갔다!" 다음 문제  └─→ 다음 문제 or 홈
```

### 3.2 챕터 구성 — 타입 기준 (책과 동일)

책이 타입별로 실려 있어서 챕터도 타입으로 나눈다. 순서는 책의 페이지 데이터에서 뽑았다.

| 순서 | 타입 | 책 페이지 | 마리 수 | | 순서 | 타입 | 책 페이지 | 마리 수 |
|---|---|---|---|---|---|---|---|---|
| 1 | 풀 | 12–22 | 97 | | 10 | 에스퍼 | 85–92 | 82 |
| 2 | 불꽃 | 23–31 | 63 | | 11 | 바위 | 93–98 | 59 |
| 3 | 물 | 31–46 | 131 | | 12 | 강철 | 98–101 | 46 |
| 4 | 벌레 | 46–54 | 77 | | 13 | 고스트 | 102–105 | 42 |
| 5 | 노말 | 54–67 | 109 | | 14 | 얼음 | 105–108 | 34 |
| 6 | 독 | 68–71 | 64 | | 15 | 악 | 109–112 | 46 |
| 7 | 땅 | 71–74 | 64 | | 16 | 드래곤 | 113–117 | 44 |
| 8 | 전기 | 75–80 | 47 | | 17 | 비행 | 118 | 98 |
| 9 | 격투 | 81–84 | 54 | | 18 | 페어리 | 118–120 | 47 |

- **잠긴 챕터는 없다.** 처음부터 아이가 원하는 타입을 골라 풀 수 있다
- 두 타입을 가진 포켓몬은 **두 챕터에 모두** 들어간다 (합계 1,330 > 802).
  첫 번째 타입만 세면 비행 타입이 3마리밖에 안 된다
- **아무 포켓몬이나 풀기**: 802마리 전체에서 균등 무작위 출제 —
  어떤 포켓몬이든 나올 수 있다 (2만 회 표본에서 802마리 전부 등장, 구간 편차 3% 이내)
- 챕터 안에서도 순서 없이 균등 무작위. 도망간 포켓몬은 30% 확률로 다시 출제

### 3.3 퀴즈 화면 (핵심)

```
┌─────────────────────────────────┐
│  ┌──────────┐  No. ???          │
│  │    ?     │  (모습은 가린다)    │
│  │  [전기]  │  ← 타입 띠         │
│  └──────────┘                    │
│                                 │
│  단서 카드 (위에서 아래로 읽기)      │
│   ① 도감: "볼의 주머니에            │
│      전기를 모은다…" (책 문장)      │
│   ② 타입: 전기 타입이야            │
│   ③ 별명: 쥐포켓몬이라고 불러       │
│   ④ 진화: 피츄가 진화한 모습이야    │
│   ⑤ 책 76쪽에 내가 있어!           │
│                                 │
│  입력칸:  [피] [카] [__]           │
│                                 │
│  글자 타일:                       │
│  [피] [츄] [리] [카] [무] [츠]     │
│         [지우기 ⌫]               │
└─────────────────────────────────┘
```

- **힌트 카드**: 도감 설명 → 타입 → 별명 → 크기 → 고향 → 특성 → 진화 → 책 순.
  전부 한 번에 보여주되 카드 형태로 크고 읽기 쉬운 글씨(문장은 짧게).
  **기술(스킬)·위력·습득 레벨 같은 배틀 게임 수치는 넣지 않는다** — 책에도 없고
  읽기 연습에 도움이 안 된다. 진화 조건도 "레벨 36이 되면" 대신 "더 자라면"으로 적는다.
- **글자 타일**: 정답 글자 수 + 함정 글자 3개 내외. 글자 수가 입력칸으로 표시되어
  "3글자 이름이구나"를 알 수 있음. 잘못 누르면 ⌫로 지우기
- **오답 피드백**: 틀리면 흔들림 + "아깝다! 다시 읽어볼까?" / 2번째 오답엔 첫 글자 자동 공개
- **정답 연출**: 판이 열려 모습 공개 → **이름 따라 쓰기** → 몬스터볼 포획 → 도감 등록
- **3회 오답**: "앗, 도망가 버렸다!" → 부담 없이 다음 문제. 해당 포켓몬은 출제 큐에 다시 들어감

### 3.3.1 이름 따라 쓰기 (쓰기 연습)

정답을 맞히면 **패드 전체 크기** 화면이 뜨고, 이름을 한 글자씩 **획 순서대로**
따라 써야 다음 문제로 넘어간다. 읽기만이 아니라 쓰기까지 연습하게 하는 장치다.

- 글자 모양을 폰트가 아니라 **자모 획 좌표**로 들고 있어(`js/write.js`)
  안내선과 글자 모양이 항상 일치한다. 초성 → 중성 → 종성 순서
- 각 획을 4.5 간격 체크포인트로 잘라 두고, 시작점에서 출발해 순서대로 통과해야 인정
- 어긋나면 그 획만 다시. 손을 떼도 그은 만큼 남고 멈춘 자리에서 이어 쓸 수 있다
- `ㅇ`·`ㅎ`의 동그라미는 어느 방향으로 돌아도 인정 (반대로 돌면 획을 뒤집는다)
- 왼쪽(가로) / 위(세로) 작은 칸에서 획순 시범이 계속 반복 재생
- 802마리 이름 2,659글자 = 획 12,733개. 이름 하나 평균 16획, 최다 글자는 `뿔`(13획)
- 한글이 아닌 글자(`♀ ♂ 2 :`)는 획 데이터가 없어 자동으로 넘어간다
- 도망갔을 때는 쓰기 화면이 뜨지 않는다 (맞힌 것만 쓴다)

### 3.4 도감 화면 (수집의 재미)

- 탭: **전체(802)** + 타입 18개. 잠긴 탭 없음
- **잡은 포켓몬**: 컬러 이미지 + 이름. 터치하면 상세 카드(타입·키·몸무게·특성·진화·도감설명·책 페이지)
- **못 잡은 포켓몬**: `?` + "???" — 퀴즈에서 모습을 가리는 것과 같은 규칙이어야
  도감을 뒤져 답을 찾는 일이 없다
- 진행률 표시: "물 타입 37 / 131", 전체 "○○ / 802"
- **홈 히어로 = 802칸 모자이크**: 책과 같은 타입 순서로 놓고, 잡으면 그 포켓몬의
  타입 색으로 칠해진다. 빈 칸도 타입 색을 12% 섞어 타입 구간이 보인다
- **수집 보상**:
  - 타입별 20% / 50% / 100%에서 동·은·금 배지 (총 54개)
  - 마일스톤 축하 (1, 5, 10, 25, 50 … 802마리)
  - 상세 카드에서 진화 사슬이 연결되어 보임 (못 잡은 칸은 흐리게)

### 3.5 저장

- `localStorage`에 진행상황 저장 (잡은 포켓몬, 도망 기록, 배지, 마일스톤, 소리 설정)
- **초기화**: 홈 머리글의 `초기화` 버튼 → 확인창에서 `초기화`라고 **직접 입력해야**
  지우기 버튼이 켜진다 (아이가 실수로 누르지 못하게)

## 4. 데이터 파이프라인 (제작 시 1회 실행)

빌드 스크립트(Node.js)로 PokéAPI에서 802마리 데이터를 받아 정적 파일로 내장:

1. **`/pokemon-species/{1..802}`** → 한글 이름(`names[ko]`), 분류(`genera[ko]`),
   도감 설명(`flavor_text_entries` 중 `ko` — 썬/문 → 울트라썬문 → 레츠고/소드실드 순 폴백),
   진화 체인 URL
2. **`/pokemon/{id}`** → 타입, 특성, 키·몸무게 (타입·특성 이름도 한글화 —
   PokéAPI의 type/ability 리소스에 `ko` 이름 있음)
3. **`/evolution-chain/{id}`** → 진화 단계 (한글 이름으로 변환)
4. **이미지**: official-artwork PNG 802장 다운로드 → `images/` 폴더에 저장
   (실루엣은 CSS `filter: brightness(0)`로 처리 — 별도 이미지 불필요)
5. 결과물: `data/pokedex.json` (약 1~2MB) + `images/*.png`

- 한글 도감 설명이 없는 포켓몬은 타입·분류 기반 자동 문장으로 폴백 ("전기 타입의 쥐포켓몬이야")
- 어려운 한자어·긴 문장은 최종 검수 단계에서 7살 눈높이로 다듬기 (일괄 후처리 규칙)

## 5. 책 페이지 매핑

- 인덱스 사진 3장(`1000032263~65.jpg`)에서 **한글 이름 → 책 페이지** 표를 추출
- PokéAPI 한글 이름과 대조해 도감 번호와 매핑 → `data/book-pages.json`
- 이름 불일치·판독 불가 항목은 목록으로 정리해 사용자 확인 요청
- 알로라 모습·메가진화는 v1에서는 기본 폼 페이지만 연동 (추후 확장)

## 6. 기술 스택 및 파일 구조

- **순수 HTML + CSS + JS** (프레임워크 없음) — 패드 브라우저에서 파일만 열면 실행
- 터치 최적화: 버튼 최소 64px, 큰 글씨(읽기 연습용 본문 22px+), 가로/세로 모두 대응
- 효과음: Web Audio API로 간단한 톤 생성 (외부 파일 없이)

```
15. 포켓몬 퀴즈/
├── plan.md
├── index.html          ← 게임 본체 (홈/퀴즈/도감/배지)
├── css/style.css
├── js/
│   ├── data.js         ← pokedex.json 로드/조회
│   ├── quiz.js         ← 출제·타일 생성·판정
│   ├── dex.js          ← 도감·진행률·배지
│   └── save.js         ← localStorage
├── data/
│   ├── pokedex.json    ← 802마리 통합 데이터
│   └── book-pages.json ← 책 페이지 매핑
├── images/             ← official artwork 802장
└── tools/
    └── build-data.mjs  ← PokéAPI 수집 스크립트 (제작용)
```

## 7. 구현 단계 — 전부 완료 ✅

| 단계 | 내용 | 결과 |
|---|---|---|
| 1 | 데이터 수집 (PokéAPI → JSON + 이미지) | ✅ 802마리 / 이미지 802장, 결측 0 |
| 2 | 인덱스 사진 → 책 페이지 매핑 | ✅ 801 / 802마리, 타입 일관성 98% |
| 3 | 퀴즈 코어: 실루엣 + 힌트 카드 + 글자 타일 + 정답/오답 연출 | ✅ |
| 4 | 도감 + 진행률 + 저장 + 챕터 잠금/해제 | ✅ |
| 5 | 배지·마일스톤·포획 연출·효과음 | ✅ 배지 21개(지역 7 × 3단계) |
| 6 | Playwright 자동 테스트 + 스크린샷 검증 | ✅ 콘솔 오류 0, 44px 미만 터치 영역 0 |

## 8. 만들면서 알게 된 것 (계획과 달랐던 부분)

- **책은 도감 번호 순이 아니라 타입별 구성이다.** 풀 12~22쪽, 불꽃 23~30쪽,
  물 31~45쪽, 벌레 46~54쪽, 노말 55쪽~ 순이라 같은 페이지에 세대가 다른
  포켓몬이 함께 실린다. 처음엔 "도감 번호가 커지면 페이지도 커진다"고 가정해
  검증 로직을 짰다가 정상 항목 453개를 버리는 것을 보고 발견했다.
  지금은 이 타입 묶음 성질을 오히려 판독 검증에 쓴다(자세한 내용은 README).
- **도감 설명의 12.6%(101마리)가 설명 안에 제 이름을 노출한다.**
  ("많은 피카츄를 모아서 발전소를…") 그대로 보여 주면 정답이 새므로 ○로 가린다.
- **`file://`로 열면 `fetch()`가 CORS로 막힌다.** 그래서 데이터를 JSON이 아니라
  `window.POKEDEX = {...}` 형태의 `.js`로 내보내 `<script>`로 읽는다.
  패드에서는 LAN 서버(`serve.mjs`)로 여는 쪽이 가장 확실하다.
- **기술(스킬)·위력·습득 레벨은 결국 다 뺐다** (아이 요청). 배틀 게임 수치라
  책에도 없고 읽기 연습에도 도움이 안 된다. 진화 조건의 "레벨 36이 되면"도
  "더 자라면"으로 바꿨다. 데이터는 남아 있지만 화면에 쓰지 않는다.
- **축하창은 새로 넘은 것 중 가장 높은 하나만 띄운다.** 여러 단계를 한 번에
  넘으면 "1마리 달성 / 5마리 달성 / 10마리 달성…"이 줄줄이 떠서 아이가 지친다.
- **한글 조사·인용형 처리가 필요하다.** 받침 유무에 따라 은/는·이/가·을/를·
  (으)로·이라고/라고를 골라야 "화염포켓몬이야라고 불러" 같은 문장이 안 나온다.
- 배지는 100% 완주만 주면 너무 멀어서 20% / 50% / 100% 3단계로 나눴다.
- **챕터를 지방에서 타입으로 바꿨다** (아이 요청). 책이 타입별 구성이라 오히려
  이게 맞았고, 잠금도 모두 풀어 원하는 타입을 바로 고를 수 있게 했다.
  타입 순서는 책의 페이지 데이터에서 뽑아 책과 같게 맞췄다.
- **출제를 완전 무작위로 바꿨다.** 처음엔 도감 번호 앞쪽 8마리에서 골랐는데,
  그러면 뒷번호 포켓몬이 한참 뒤에야 나온다. 지금은 아직 안 잡은 전체에서 균등 추출.
- **실루엣을 없앴다.** 모습만 보고도 맞힐 수 있어 단서를 읽지 않았다.
  지금은 `No. ???`와 `?`만 두고, 맞히거나 도망갔을 때 공개한다.
  도감의 미획득 칸도 같은 규칙(`?`)이어야 도감을 뒤져 답을 찾는 일이 없다.

- **캐릭터 그림은 한 배율로 잘라야 한다.** 20명을 각자 꼭 맞게 잘라 같은 높이로
  늘리니 키 비율이 뒤죽박죽이 되고, 가로 비율이 달라 고르는 화면의 칸 높이까지
  어긋났다. 지금은 가장 큰 캐릭터 기준 **공통 배율 + 같은 크기 캔버스 + 바닥선 정렬**.
- **절대 위치 이미지에 `width:auto`를 주면 안 된다.** '남은 자리 채우기'가 아니라
  '원본 크기'가 쓰이고 `right`는 무시돼 카드를 넘쳐 잘린다. 반대로 그리드 항목으로
  두면 원본 크기가 칸을 늘려 `aspect-ratio`가 깨진다 — 크기를 명시하는 게 답이다.

- **섞임모음(ㅘㅝㅢ…)은 가로 부분과 세로 부분을 따로 앉혀야 한다.** 한 상자에
  통째로 넣으니 가로 막대(ㅗ/ㅜ/ㅡ)가 초성 자리를 관통해 휠·활·쉘·화가 뭉개졌다.
  나눠 앉히면 획순(초성 → 가로모음 → 세로모음 → 받침)까지 자연히 맞는다.
  덧붙여 획이 많은 글자는 펜을 얇게 해야 좁은 받침 칸의 ㄹ·ㅁ 가로줄이 붙지 않는다.
- **동기 부여는 '다음 한 걸음'이 보여야 생긴다.** 802마리는 일곱 살에게 너무 멀어서
  8단계 트레이너 등급(시작 → 전설)을 얹고 홈 제목 자리에 두었다. 초반 칸을
  10·30마리로 좁게 잡아 시작하자마자 한 번 올라가 보게 했고, 늘 "다음 등급까지
  N마리 더"를 함께 보여 준다. 모은 배지는 캐릭터 밑 진열장에 쌓아 눈에 보이게 했다.
- **같은 '몇 마리' 이야기로 축하창을 두 번 띄우지 않는다.** 등급과 마일스톤이
  겹치는 지점(10·150·300·802)에서는 등급 승급만 알린다.

- **단서를 한 번에 다 깔아 놓으면 도감 설명을 안 읽는다.** 가장 긴 글(도감)을
  건너뛰고 아래의 짧은 단서만 훑는다. 그래서 문제마다 도감 설명 하나만 먼저 두고
  **15초 동안 '다음 힌트 확인'을 잠근다**(남은 초 + 차오르는 막대). 다 차면 눌러서
  남은 단서를 한꺼번에 펼친다. 정답 입력은 잠그지 않았고 — 이미 아는 아이를
  기다리게 할 이유가 없다 — 틀리면 도움이 필요한 순간이므로 바로 펼쳐 준다.
- **가로 화면 그리드에서는 `.clues` 같은 공통 클래스로 칸을 지정하면 안 된다.**
  단서 상자를 둘로 나눈 뒤 같은 규칙이 둘 다에 걸려 같은 칸에 겹쳤다. `#hints`처럼
  아이디로 짚어야 한다.

## 9. v1 범위에서 제외 (추후 후보)

- 알로라 모습·메가진화 별도 도감 카드 (색인에는 페이지가 있으나 기본 폼으로만 연결)
- 음성 읽어주기(TTS), 2인 대전 모드
- 잡은 포켓몬으로 하는 미니게임 (타입 상성 배틀 등)
- 아이가 실제로 해 본 뒤 조정할 것:
  - 글자 타일의 함정 글자 개수 (현재 3개)
  - 목숨 수 (현재 3개)
  - **따라 쓸 분량** (현재 이름 전체 = 평균 16획). 길다면 첫 글자만,
    또는 한 글자만 무작위로 고르게 줄일 수 있다 (`js/quiz.js`의 `Write.open` 인자)
