# 포켓몬 도감 퀴즈 게임 — 계획서 (plan.md)

## 1. 목표

책 **《포켓몬스터 썬&문 포켓몬 전국대도감》**(1~7세대, 802마리)을 읽은 7살 아이가
**책 내용을 떠올리며 퀴즈를 풀고, 맞출 때마다 나만의 도감이 채워지는** 웹 게임.

- 퀴즈 힌트(타입·특성·기술·진화·도감 설명)를 **소리내어 읽는 것 자체가 읽기 연습**이 되도록 구성
- 도감을 한 마리씩 모으는 **수집의 재미**가 핵심 동기
- 책과 연동: 포켓몬마다 "책 ○○쪽에서 찾아봐!" 표시 (인덱스 사진 3장에서 추출)

## 2. 인터뷰로 확정된 사항

| 항목 | 결정 |
|---|---|
| 플랫폼 | **웹앱** — HTML/JS, 패드 브라우저에서 실행. 설치 불필요 |
| 정답 입력 | **글자 타일 조합** — 정답 글자 + 함정 글자를 큰 버튼으로 섞어 제시, 순서대로 터치해 이름 완성 |
| 도감 범위 | **전체 802마리**, 지역(세대)별 챕터로 순차 오픈 |
| 데이터 | **PokéAPI에서 미리 다운로드해 내장** — 플레이 시 인터넷 불필요, 로딩 빠름 |
| 문제 화면 | **실루엣 → 정답 시 컬러 공개** ("이 포켓몬은 누구지?" 스타일) |
| 오답 처리 | **3번 틀리면 "도망갔다!"** 처리 후 다음 문제로. 도망간 포켓몬은 나중에 다시 등장 |

## 3. 게임 구성

### 3.1 화면 흐름

```
[홈] ─── 지역(챕터) 선택 ───→ [퀴즈 화면] ──정답──→ [포획 연출 + 도감 등록]
  │                              │ 3회 오답              │
  └──→ [내 도감]                 └─→ "도망갔다!" 다음 문제  └─→ 다음 문제 or 홈
```

### 3.2 지역(챕터) 구성 — 책의 순서와 동일

| 챕터 | 지역 | 도감 번호 | 마리 수 |
|---|---|---|---|
| 1 | 관동 | 001–151 | 151 |
| 2 | 성도 | 152–251 | 100 |
| 3 | 호연 | 252–386 | 135 |
| 4 | 신오 | 387–493 | 107 |
| 5 | 하나 | 494–649 | 156 |
| 6 | 칼로스 | 650–721 | 72 |
| 7 | 알로라 | 722–802 | 81 |

- 처음엔 **관동만 열림**. 챕터에서 일정 수(예: 30마리)를 잡으면 다음 지역 열림
- 표지 주인공(피카츄·나몰빼미·냐오불·누리공 등)이 안내 캐릭터로 등장하면 좋음
- 각 챕터 안에서 문제는 랜덤 출제 (아직 못 잡은 포켓몬 우선)

### 3.3 퀴즈 화면 (핵심)

```
┌─────────────────────────────────┐
│   검은 실루엣 이미지 (크게)         │
│                                 │
│  💡 힌트 카드 (하나씩 넘겨 읽기)     │
│   ① 타입: 전기 타입이야            │
│   ② 특성/기술: 10만볼트를 써!      │
│   ③ 진화: 라이츄로 진화해          │
│   ④ 도감 설명: "볼의 주머니에       │
│      전기를 모은다…" (책 스타일)    │
│   📖 책 76쪽에서 찾아봐!           │
│                                 │
│  입력칸:  [피] [카] [__]           │
│                                 │
│  글자 타일:                       │
│  [피] [츄] [리] [카] [무] [츠]     │
│         [지우기 ⌫]               │
└─────────────────────────────────┘
```

- **힌트 카드**: 타입 → 특성·기술 → 진화 → 도감 설명 순으로 배치. 전부 한 번에 보여주되
  카드 형태로 크고 읽기 쉬운 글씨(문장은 짧게). 최대한 자세한 정보 제공 = 읽을거리
- **글자 타일**: 정답 글자 수 + 함정 글자 3개 내외. 글자 수가 입력칸으로 표시되어
  "3글자 이름이구나"를 알 수 있음. 잘못 누르면 ⌫로 지우기
- **오답 피드백**: 틀리면 흔들림 + "아깝다! 다시 읽어볼까?" / 2번째 오답엔 첫 글자 자동 공개
- **정답 연출**: 실루엣이 컬러로 짠! + 몬스터볼 포획 애니메이션 + 효과음 + "○○를 잡았다!"
- **3회 오답**: "앗, 도망가 버렸다!" → 부담 없이 다음 문제. 해당 포켓몬은 출제 큐에 다시 들어감

### 3.4 도감 화면 (수집의 재미)

- 지역별 탭, 번호 순 그리드
- **잡은 포켓몬**: 컬러 이미지 + 이름. 터치하면 상세 카드(타입·키·몸무게·기술·진화·도감설명·책 페이지)
- **못 잡은 포켓몬**: 검은 실루엣 + "???" → 뭐가 남았는지 보여서 모으고 싶게
- 진행률 표시: "관동 37 / 151" 게이지, 전체 "○○ / 802"
- **수집 보상**:
  - 지역 완성 시 배지 획득 (배지 진열장 화면)
  - 마일스톤 축하 연출 (10마리, 50마리, 100마리…)
  - 진화 라인을 전부 모으면 도감에서 진화 사슬이 연결되는 연출

### 3.5 저장

- `localStorage`에 진행상황 저장 (잡은 포켓몬, 배지, 열린 챕터)
- 실수로 지워지지 않게 "도감 초기화"는 설정 깊숙이 + 확인 2회

## 4. 데이터 파이프라인 (제작 시 1회 실행)

빌드 스크립트(Node.js)로 PokéAPI에서 802마리 데이터를 받아 정적 파일로 내장:

1. **`/pokemon-species/{1..802}`** → 한글 이름(`names[ko]`), 분류(`genera[ko]`),
   도감 설명(`flavor_text_entries` 중 `ko` — 썬/문 → 울트라썬문 → 레츠고/소드실드 순 폴백),
   진화 체인 URL
2. **`/pokemon/{id}`** → 타입, 특성, 대표 기술 2~3개 (타입·특성·기술 이름도 전부 한글화 —
   PokéAPI의 type/ability/move 리소스에 `ko` 이름 있음), 키·몸무게
3. **`/evolution-chain/{id}`** → 진화 단계 (한글 이름으로 변환)
4. **이미지**: official-artwork PNG 802장 다운로드 → `images/` 폴더에 저장
   (실루엣은 CSS `filter: brightness(0)`로 처리 — 별도 이미지 불필요)
5. 결과물: `data/pokedex.json` (약 1~2MB) + `images/*.png`

- 한글 도감 설명이 없는 포켓몬은 타입·분류 기반 자동 문장으로 폴백 ("전기 타입의 쥐포켓몬이야")
- 어려운 한자어·긴 문장은 최종 검수 단계에서 7살 눈높이로 다듬기 (일괄 후처리 규칙)

## 5. 책 페이지 매핑

- 인덱스 사진 3장(`1000032263~65.jpg`)에서 **한글 이름 → 책 페이지** 표를 추출
- PokéAPI 한글 이름과 대조해 도감 번호와 매핑 → `data/book-pages.json`
- 이름 불일치·판독 불가 항목은 목록으로 정리해 사용자 확인 요청
- 알로라 모습·메가진화는 v1에서는 기본 폼 페이지만 연동 (추후 확장)

## 6. 기술 스택 및 파일 구조

- **순수 HTML + CSS + JS** (프레임워크 없음) — 패드 브라우저에서 파일만 열면 실행
- 터치 최적화: 버튼 최소 64px, 큰 글씨(읽기 연습용 본문 22px+), 가로/세로 모두 대응
- 효과음: Web Audio API로 간단한 톤 생성 (외부 파일 없이)

```
15. 포켓몬 퀴즈/
├── plan.md
├── index.html          ← 게임 본체 (홈/퀴즈/도감/배지)
├── css/style.css
├── js/
│   ├── data.js         ← pokedex.json 로드/조회
│   ├── quiz.js         ← 출제·타일 생성·판정
│   ├── dex.js          ← 도감·진행률·배지
│   └── save.js         ← localStorage
├── data/
│   ├── pokedex.json    ← 802마리 통합 데이터
│   └── book-pages.json ← 책 페이지 매핑
├── images/             ← official artwork 802장
└── tools/
    └── build-data.mjs  ← PokéAPI 수집 스크립트 (제작용)
```

## 7. 구현 단계 — 전부 완료 ✅

| 단계 | 내용 | 결과 |
|---|---|---|
| 1 | 데이터 수집 (PokéAPI → JSON + 이미지) | ✅ 802마리 / 이미지 802장, 결측 0 |
| 2 | 인덱스 사진 → 책 페이지 매핑 | ✅ 801 / 802마리, 타입 일관성 98% |
| 3 | 퀴즈 코어: 실루엣 + 힌트 카드 + 글자 타일 + 정답/오답 연출 | ✅ |
| 4 | 도감 + 진행률 + 저장 + 챕터 잠금/해제 | ✅ |
| 5 | 배지·마일스톤·포획 연출·효과음 | ✅ 배지 21개(지역 7 × 3단계) |
| 6 | Playwright 자동 테스트 + 스크린샷 검증 | ✅ 콘솔 오류 0, 44px 미만 터치 영역 0 |

## 8. 만들면서 알게 된 것 (계획과 달랐던 부분)

- **책은 도감 번호 순이 아니라 타입별 구성이다.** 풀 12~22쪽, 불꽃 23~30쪽,
  물 31~45쪽, 벌레 46~54쪽, 노말 55쪽~ 순이라 같은 페이지에 세대가 다른
  포켓몬이 함께 실린다. 처음엔 "도감 번호가 커지면 페이지도 커진다"고 가정해
  검증 로직을 짰다가 정상 항목 453개를 버리는 것을 보고 발견했다.
  지금은 이 타입 묶음 성질을 오히려 판독 검증에 쓴다(자세한 내용은 README).
- **도감 설명의 12.6%(101마리)가 설명 안에 제 이름을 노출한다.**
  ("많은 피카츄를 모아서 발전소를…") 그대로 보여 주면 정답이 새므로 ○로 가린다.
- **`file://`로 열면 `fetch()`가 CORS로 막힌다.** 그래서 데이터를 JSON이 아니라
  `window.POKEDEX = {...}` 형태의 `.js`로 내보내 `<script>`로 읽는다.
  패드에서는 LAN 서버(`serve.mjs`)로 여는 쪽이 가장 확실하다.
- **힌트용 기술은 위력이 아니라 "레벨업으로 스스로 배우는 기술"로 골라야 한다.**
  위력만 보면 아무 포켓몬이나 배우는 기술머신 기술이 뽑혀 힌트가 밋밋해진다
  (피카츄에 10만볼트 대신 번개).
- **축하창은 새로 넘은 것 중 가장 높은 하나만 띄운다.** 여러 단계를 한 번에
  넘으면 "1마리 달성 / 5마리 달성 / 10마리 달성…"이 줄줄이 떠서 아이가 지친다.
- **한글 조사·인용형 처리가 필요하다.** 받침 유무에 따라 은/는·이/가·을/를·
  (으)로·이라고/라고를 골라야 "화염포켓몬이야라고 불러" 같은 문장이 안 나온다.
- 배지는 지역 100% 완주만 주면 관동 151마리가 너무 멀어서, 20% / 50% / 100%
  3단계로 나눴다(총 21개).

## 9. v1 범위에서 제외 (추후 후보)

- 알로라 모습·메가진화 별도 도감 카드 (색인에는 페이지가 있으나 기본 폼으로만 연결)
- 음성 읽어주기(TTS), 2인 대전 모드
- 잡은 포켓몬으로 하는 미니게임 (타입 상성 배틀 등)
- 아이가 실제로 해 본 뒤 조정할 것: 다음 지방 해제 조건(현재 30마리),
  글자 타일의 함정 글자 개수(현재 3개), 목숨 수(현재 3개)
